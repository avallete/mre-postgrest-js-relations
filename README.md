# mre-invalid-relations

To install dependencies:

```bash
bun install
bunx supabase start
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.1.27. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.
